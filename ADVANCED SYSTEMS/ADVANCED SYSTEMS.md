Continuous = contact signal/pulses of 0/1
Discrete = single pulse of 0/1 per bit




below is being debated

Static  = no move, tween, bot movement, does it take compute power when doing nothing / off
Dynamic = includes above ^
